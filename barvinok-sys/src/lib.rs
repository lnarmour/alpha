//! Raw FFI bindings to libbarvinok.
//!
//! GPL-licensed (unlike `isl-sys`/`isl`, which are MIT) — see this crate's `Cargo.toml`. Not yet
//! implemented; nothing outside an explicit `barvinok` Cargo feature may depend on this crate.
