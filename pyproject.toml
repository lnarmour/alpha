[project]
name = "alpha-rs"
version = "0.0.0"
requires-python = ">=3.9"
dependencies = ["alpha"]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["alpha-py"]

[tool.uv.sources]
alpha = { workspace = true }

[dependency-groups]
dev = ["prek", "pytest", "nbval", "ipython", "jupyterlab"]
