[package]
name = "isl-sys"
version = "0.1.0"
edition.workspace = true
license = "MIT"
description = "Raw FFI bindings to libisl (the Integer Set Library)."
build = "build.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
bindgen = "0.70"
pkg-config = "0.3"
