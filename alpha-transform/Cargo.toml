[package]
name = "alpha-transform"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "AST-to-AST transformations the codegen path depends on (Normalize, NormalizeReduction)."

[dependencies]
alpha-syntax.workspace = true
alpha-model.workspace = true
isl.workspace = true
