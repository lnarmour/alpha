[package]
name = "barvinok"
version = "0.1.0"
edition.workspace = true
license = "GPL-2.0-or-later"
description = "Safe Rust wrapper over libbarvinok (Ehrhart/cardinality counting). GPL — opt-in only."

[dependencies]
barvinok-sys.workspace = true
isl.workspace = true
