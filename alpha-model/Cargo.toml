[package]
name = "alpha-model"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Semantic model and six-phase checker for the Alpha language (name/domain resolution, diagnostics)."

[dependencies]
alpha-syntax.workspace = true
isl.workspace = true
